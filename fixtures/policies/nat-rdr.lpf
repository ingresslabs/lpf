set default deny

interface lan = "eth1"
interface wan = "eth0"

nat on wan from 10.0.0.0/24 to any -> wan
rdr on wan proto tcp from any to any port 80 -> 10.0.0.5 port 8080

pass in on lan from 10.0.0.0/24 to any
pass out on wan proto tcp from any to any port 443 keep state
